# dependencies
node_modules/
client/node_modules/

# build output
dist/
.vite/

# secrets — .env holds live Atlas credentials and the JWT secret.
# Only .env.example is tracked.
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*

# test / audit artefacts
coverage/
test-results/
playwright-report/

# Working documentation. Every spec, the progress log and the client brief live
# in docs/ and are kept local — CLAUDE.md at the root is ignored with them.
docs
CLAUDE.md

# scratch
scripts

# OS
.DS_Store
Thumbs.db

# playwright-cli session artifacts (snapshots, console logs)
.playwright-cli/


.tmp.drivedownload
.tmp.driveupload

# Database exports (password hashes, customer records). Never committed.
backups/
